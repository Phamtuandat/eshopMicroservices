using Background.API.Services;
using BuildingBlocks.Messaging.Events;
using MassTransit;

namespace Background.API.EventHandlers.cs
{
  /*  public class SentOTPEventHandler(IEmailService emailService) : IConsumer<SentOTPEvent>
    {
        *//*private readonly IEmailService _emailService = emailService;
        public async Task Consume(ConsumeContext<SentOTPEvent> context)
        {
            var message = context.Message;
            _emailService.SendOTPEmailAsync(message.Code, message.Email);

        }*//*
    }*/
}
