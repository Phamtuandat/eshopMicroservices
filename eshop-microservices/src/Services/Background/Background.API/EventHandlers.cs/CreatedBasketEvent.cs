using BuildingBlocks.Messaging.Events;
using Hangfire;
using MassTransit;

namespace Background.API.EventHandlers.cs
{
  /*  public class CreatedBasketEvent : IConsumer<CheckedOutEvent>
    {
        public async Task Consume(ConsumeContext<CheckedOutEvent> context)
        {
            
            BackgroundJob.Enqueue(() => Console.WriteLine(context.Message.Id.ToString()));
        }
    }*/
}
